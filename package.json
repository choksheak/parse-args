{
  "name": "@choksheak/parse-args",
  "description": "See publish/package.json",
  "scripts": {
    "minify":"uglifyjs --compress --mangle --toplevel --output publish/index.js -- index.js",
    "test": "node test.js"
  },
  "license": "ISC",
  "devDependencies": {
    "uglify-js": "^3.12.2"
  }
}
